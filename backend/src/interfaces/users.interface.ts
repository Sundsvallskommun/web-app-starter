export type User = {
  id: number;
  username: string;
  name: string;
  givenName: string;
  surname: string;
  guid: string;
};

export type ClientUser = {
  name: string;
  username: string;
};
