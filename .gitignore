# Root-level dependencies (husky / lint-staged / commitlint orchestration).
/node_modules

# Next.js generates this with environment-specific route type imports.
next-env.d.ts

# Husky internals (regenerated by `husky` on install).
.husky/_

# OS / editor cruft
.DS_Store
*.log
